export class Ingredient {
    // name: string;
    // amount: number;

    constructor(public name: string, public amount: number) { // added public, good ts shorcut
        // this.name = name;
        // this.amount = amount;
    }
}